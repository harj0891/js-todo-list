
let text = () => {
    console.log("test");
}

text();

/* item
- name
- description/notes
- status (pending/done)
- due date (optional)
- order (optional) -- can later introduce drag/drop 
- project (can only belong to 1 project, can't be changed)
*/


/* project
- name
- description (optional)
- progress (in %)
- due date (optional)
- order (optional) -- can later introduce drag/drop 
- priority (optional) -- will change project label color
- headings (optional) -- to group items 
*/